# as minimal as possible
from setuptools import setup, find_packages


setup(
    name = 'dicom_numpy',
    version = '10.0.0',
    url = 'https://github.com/mmikolajczak/dicom_numpy_fork',
    description = 'Fork of dicom_numpy package, with a couple of modifications/fixes',
    packages = find_packages()
)
